# 黄山管理后台 - 任务执行追踪列表

## 订单管理重构模块 [已完成]
- [x] 素材订单表格移除“退款”按钮，仅保留“详情”按钮
- [x] 确权订单表格新增“退款”按钮和“详情”按钮
- [x] 重构素材订单和确权订单的“详情”按钮为在当前页弹出详情模态框，包含详细的订单与交易流水数据，并根据文件类型联动渲染对应的图像、视频或文章预览
- [x] 确权订单的证书状态筛选框中新增“已退款”选项
- [x] 退款时，联动注销 `hscms_asset_submissions` 表中对应作品的 DCI 登记码（修改为 `-`），并将证书编号 `certNo` 清空为 `-`
- [x] 实现作品名称到资产 ID 的匹配自愈，支持防报错防红屏动态生成模拟数据

## 通知公告发布系统 [已完成]
- [x] **Phase 1: 全局导航与结构搭建**
  - [x] 修改 `settings.html` 的二级侧边栏，新增“通知公告”菜单项（指向 `announcements.html`）
- [x] **Phase 2: 公告管理页面 (announcements.html) 界面开发**
  - [x] 新建 `announcements.html` 页面，沿用后台经典视觉规范（含侧边栏及高亮）
  - [x] 在右侧主工作区绘制通知公告历史日志表格（ID、标题、发布范围、发布时间、操作）
  - [x] 新增“发布通知”操作按钮
- [x] **Phase 3: 发布公告弹窗与选择范围功能开发**
  - [x] 编写“发布新公告”模态框 DOM 结构
  - [x] 包含标题输入框、富文本消息编辑器（支持加粗、倾斜、下划线及文本颜色的基础富文本排版）
  - [x] 包含单选钮切换推送范围（全部 / 部分）
  - [x] 编写“选择部分用户”时的联动滑出面板，包含带有复选框、搜索过滤功能的完整用户列表
- [x] **Phase 4: 数据存储与用户消息流转联动 (LocalStorage)**
  - [x] 编写发布逻辑：在 `hscms_announcements` 表中记录公告发布历史
  - [x] 实现循环逻辑：将公告内容转换为系统消息写入 `hscms_system_messages`
  - [x] 添加发布前置防御性校验（如未选人、空内容等）
  - [x] 编写公告详情弹窗，允许管理员查看富文本公告的历史渲染形态与接收人名单
- [x] **Phase 5: 联调与测试**
  - [x] 验证推送范围为“全部”时所有用户均可收到消息
  - [x] 验证推送范围为“部分”时仅选中用户收到消息，且能正常过滤
  - [x] 确保富文本排版效果能完美展示

- [x] **Phase 5: 重命名用途配置并支持价格配置**
  - [x] 修改 `settings.html` 中的文案：“用途配置”改名为“素材用途配置”，“版权交易用途树形配置”改名为“素材交易用途配置”
  - [x] 重构 `loadUsageTree` 渲染：支持显示子规格名及价格 `规格名 (￥价格)`，兼容老数据
  - [x] 修改 `childSpecModal` 的弹窗 HTML 结构，新增“用途配置价格（元）”数值输入框
  - [x] 调整子级新增、编辑、回显与保存逻辑，将子规格保存为 `{ name, price }` 对象并持久化

- [x] **Phase 6: 订单管理模块功能重构**
  - [x] 修改 `orders.html` 的素材订单表格：
    - 彻底移除了素材交易订单的“退款”操作按钮与逻辑
    - 新增“详情”链接按钮，支持在当前页面弹窗展示“素材订单详情”（包含支付信息、订单状态以及素材内容的多媒体/文档预览）
  - [x] 修改 `orders.html` 的存证确权订单表格：
    - 新增“详情”链接按钮，支持在当前页面弹窗展示“确权订单详情”（包含确权金额、主体、DCI证书码、存证区块ID及验证回执）
    - 新增“退款”操作按钮（仅非“已退款”状态下显示）
    - 确权订单的证书状态筛选下拉框中加入“已退款”选项
    - 状态为“已退款”的确权订单状态徽标渲染为红色 `badge-danger`，证书编号显示为 `-`
  - [x] 编写 `refundProofOrder` 逻辑：
    - 点击退款弹出确认窗口，确认后更新确权订单状态为“已退款”，证书编号 `certNo` 设为 `-`
    - 联动修改该订单对应的审核资产的 `dciCode` 为 `-`，注销该作品的确权登记码并存入本地存储
  - [x] 优化名称到资产 ID 的匹配（`getAssetIdByName`），支持自动防报错防红屏动态生成模拟数据

- [x] **Phase 7: 通知公告发布系统**
  - [x] 搭建通知公告发布主页面 `announcements.html`，实现卡片日志列表与每页 5 条分页
  - [x] 编写原生富文本编辑器面板（支持 B、I、U、字号、颜色配置）
  - [x] 开发推送范围条件切换面板，支持面向全体或特定部分用户（提供带勾选与模糊搜索过滤的用户选择器）
  - [x] 编写数据流转，发布时自动将富文本内容写入 `hscms_announcements` 日志并循环在 `hscms_system_messages` 中生成未读消息

- [x] **Phase 8: 用户申请下架通知与铃铛通知中心 [已完成]**
  - [x] **数据初始化与核心流转 (js/common.js)**：
    - [x] 在 `initDatabase` 中初始化并预设 `hscms_take_down_requests` 数据表
    - [x] 实现全局红点监测与红点自动更新逻辑函数 `checkTakeDownNotifications`
  - [x] **页面导航与下架申请页面开发 (take-down-requests.html)**：
    - [x] 新建 `take-down-requests.html` 页面，沿用素材确权类别的二级侧边栏风格，高亮“用户申请下架通知”子菜单
    - [x] 开发下架申请通知的卡片式展示列表，提供申请下架的素材 ID、名称、提报作者、下架理由、申请时间以及未读/已读标识
    - [x] 头部提供包含关键词（素材名称/作者名）与已读/未读状态的组合筛选器
    - [x] 操作列提供“已读”操作按钮，点击后订单标识切换为“已读”并更新全局红点
  - [x] **首页去搜索化与右上角通知铃铛挂载 (所有 HTML 页面)**：
    - [x] 修改 `index.html` 页面，彻底移除头部中间的搜索输入框功能组件
    - [x] 在所有核心 HTML 页面的头部 `header-right` 模块中的头像左侧增加一个具备红点提醒功能的原生铃铛组件（红点显示与全局持久化未读状态联动，点击后跳转至 `take-down-requests.html`）
  - [x] **多页面子菜单导航同步升级**：
    - [x] 同步修改 `asset-audit.html` 和 `take-down-requests.html` 的二级侧边栏，均包含：待审核队列、确权历史、已驳回记录、用户申请下架通知 (高亮高亮联动)
  - [x] **测试与联调**：
    - [x] 验证已读操作能触发全局红点消除
    - [x] 验证首页搜索组件成功被完全隐藏
    - [x] 确保页面跳转无空白报错
    - [x] 确保铃铛按钮在用户头像的左侧展示，且外边距保持美观对齐

- [x] **Phase 9: 页面文案净化与用户违规重置交互优化 [已完成]**
  - [x] **ugc.html 文案去除**：下架状态下不展示“已人工干预”字样
  - [x] **asset-audit.html 文案去除**：非待审核状态下不展示“已归档”字样
  - [x] **take-down-requests.html 文案去除**：已读状态下不展示“已查阅”字样
  - [x] **finance.html 文案去除**：清算拦截状态下不展示“⚠️ 清算受限”字样
  - [x] **user-audit.html 交互与文案优化**：
    - [x] 资质审核非待审核状态下不展示“已核验归档”字样
    - [x] 优化 `loadUsersList` 过滤逻辑，重置违规信息的用户（简介已被更改为重置状态）自动在合规列表中隐去，无需手动刷新
  - [x] **手动联调与走查**：
    - [x] 校验各修改页面的操作按钮布局无异常，保留的详情等超链接仍可正常点击并起效
    - [x] 验证用户被重置违规后，合规检测列表已将其自动排除
